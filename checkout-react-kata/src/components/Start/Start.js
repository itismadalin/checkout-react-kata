import React from 'react';
import './Start.css';

const Start = () => {
    return (
        <div className="button">
            <button>START</button>
        </div>
    );
};

export default Start;